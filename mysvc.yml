apiVersion: v1
kind: Service
metadata:
  name: mymginx-svs
  labeles:
       app: mynginx
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30001
    protocal: TCP
  selector:
      app: mynginx     

